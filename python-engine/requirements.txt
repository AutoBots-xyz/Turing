# Core API framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
python-multipart==0.0.12
python-dotenv==1.0.1

# HTTP client (search engines)
httpx==0.27.2

# Graph math (Layer 3 isomorphism)
networkx==3.4.2

# LLM integration (swap between Claude, GPT-4o, Ollama)
litellm==1.50.0

# Causal graph building (Text Path)
# sentence-transformers==3.1.1  # Uncomment when SPECTER2 embedding is implemented

# Causal discovery (Data Path — CSV → PC Algorithm)
causal-learn==0.1.4.0

# Agent simulation loop (Layer 2)
scikit-learn==1.5.2
numpy==2.1.2

# Do-Calculus interventions (Layer 2)
dowhy==0.12

# Database (SQLite default, PostgreSQL-ready)
sqlalchemy==2.0.36

# Async SQLite support
aiosqlite==0.20.0
